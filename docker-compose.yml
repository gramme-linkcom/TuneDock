services:
  tunedock:
    build:
      context: .
      dockerfile: backend/Dockerfile
    working_dir: /app/backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
      - ./Music:/music:ro
      - ./Musics:/musics:ro
    command: go run ./cmd/tunedock
